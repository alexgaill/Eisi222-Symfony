<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		{# Run `composer require symfony/webpack-encore-bundle`
		           and uncomment the following Encore helpers to start using Symfony UX #}
		{% block stylesheets %}
			{#{{ encore_entry_link_tags('app') }}#}
		{% endblock %}

		{% block javascripts %}
			<link rel="stylesheet" href={{asset("css/style.css")}}>
			{#{{ encore_entry_script_tags('app') }}#}
		{% endblock %}
	</head>
	<body>
		<header>
			<nav>
                <a href={{path('index')}}>Accueil</a>
                <a href={{path('articles')}}>Articles</a>
                <a href={{path('saveArticle')}}>Ajouter</a>
				{% if is_granted("ROLE_USER") %}
                	<a href={{path('app_logout')}}>Déconnexion</a>
				{% else %}
                	<a href={{path('app_login')}}>Se connecter</a>
				{% endif %}
            </nav>
		</header>
		<main>
		{# {% set name = "John" %}
		<h1>{% trans with {'%name%': 'Fabien'} %} Bonjour %name% {% endtrans %}</h1> #}
		{# {% trans %}
			<h3>foo</h3>
		{% endtrans %}
		{{ '<h3>foo</h3>'|trans|raw }} #}

		{% block body %}{% endblock %}
		</main>
		<footer>
			<p>Site créé lors du cours de Symfony</p>
		</footer>
	</body>
</html>
