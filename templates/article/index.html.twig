{% extends 'base.html.twig' %}

{% block title %}Hello ArticleController!{% endblock %}

{% block body %}
{{dump(articles)}}
{# {% if articles is not empty %}
    <ul>
        {% for article in articles %}
            <li>{{article.title}}</li>
        {% endfor %}
    </ul>
{% endif %} #}
<ul>
        {% for article in articles %}
            <li><a href={{path('article', {'id': article.id})}}>{{article.upperTitle}}</a> | categorie: {{article.categorie.name}}</li>
        {% else %}
            <li>Pas d'articles de disponible</li>
        {% endfor %}
    </ul>
{% endblock %}
